TARGET_DIR=tmp

prepare:
	mkdir -p ${TARGET_DIR}

install: prepare
	cp -rf erlang-rfc4627 ${TARGET_DIR}
	cp -rf ibrowse ${TARGET_DIR}
	cp -rf rabbitmq ${TARGET_DIR}
	cp -rf rabbitmq-erlang-client ${TARGET_DIR}


